_schema-version: "3.2"
ID: productos
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: productos-destination-content
  type: com.sap.application.content
  requires:
  - name: productos-destination-service
    parameters:
      content-target: true
  - name: productos-repo-host
    parameters:
      service-key:
        name: productos-repo-host-key
  - name: productos-uaa
    parameters:
      service-key:
        name: productos-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: productos_html_repo_host
          ServiceInstanceName: productos-html5-service
          ServiceKeyName: productos-repo-host-key
          sap.cloud.service: productos
        - Authentication: OAuth2UserTokenExchange
          Name: productos_uaa
          ServiceInstanceName: productos-xsuaa-service
          ServiceKeyName: productos-uaa-key
          sap.cloud.service: productos
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: productos-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: productos-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - productos.zip
      name: productos
      target-path: resources/
- name: productos
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: productos-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: productos-destination-service
    service-plan: lite
- name: productos-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: productos-xsuaa-service
    service-plan: application
- name: productos-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: productos-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
