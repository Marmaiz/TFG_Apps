_schema-version: "3.2"
ID: calibres
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: calibres-destination-content
  type: com.sap.application.content
  requires:
  - name: calibres-destination-service
    parameters:
      content-target: true
  - name: calibres-repo-host
    parameters:
      service-key:
        name: calibres-repo-host-key
  - name: calibres-uaa
    parameters:
      service-key:
        name: calibres-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: calibres_html_repo_host
          ServiceInstanceName: calibres-html5-service
          ServiceKeyName: calibres-repo-host-key
          sap.cloud.service: calibres
        - Authentication: OAuth2UserTokenExchange
          Name: calibres_uaa
          ServiceInstanceName: calibres-xsuaa-service
          ServiceKeyName: calibres-uaa-key
          sap.cloud.service: calibres
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: calibres-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: calibres-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - calibres.zip
      name: calibres
      target-path: resources/
- name: calibres
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: calibres-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: calibres-destination-service
    service-plan: lite
- name: calibres-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: calibres-xsuaa-service
    service-plan: application
- name: calibres-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: calibres-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
