_schema-version: "3.2"
ID: pedidos
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: pedidos-destination-content
  type: com.sap.application.content
  requires:
  - name: pedidos-destination-service
    parameters:
      content-target: true
  - name: pedidos-repo-host
    parameters:
      service-key:
        name: pedidos-repo-host-key
  - name: pedidos-uaa
    parameters:
      service-key:
        name: pedidos-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: pedidos_html_repo_host
          ServiceInstanceName: pedidos-html5-service
          ServiceKeyName: pedidos-repo-host-key
          sap.cloud.service: pedidos
        - Authentication: OAuth2UserTokenExchange
          Name: pedidos_uaa
          ServiceInstanceName: pedidos-xsuaa-service
          ServiceKeyName: pedidos-uaa-key
          sap.cloud.service: pedidos
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: pedidos-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: pedidos-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - pedidos.zip
      name: pedidos
      target-path: resources/
- name: pedidos
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: pedidos-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: pedidos-destination-service
    service-plan: lite
- name: pedidos-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: pedidos-xsuaa-service
    service-plan: application
- name: pedidos-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: pedidos-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
