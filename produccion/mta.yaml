_schema-version: "3.2"
ID: produccion
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: produccion-destination-content
  type: com.sap.application.content
  requires:
  - name: produccion-destination-service
    parameters:
      content-target: true
  - name: produccion-repo-host
    parameters:
      service-key:
        name: produccion-repo-host-key
  - name: produccion-uaa
    parameters:
      service-key:
        name: produccion-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: produccion_html_repo_host
          ServiceInstanceName: produccion-html5-service
          ServiceKeyName: produccion-repo-host-key
          sap.cloud.service: produccion
        - Authentication: OAuth2UserTokenExchange
          Name: produccion_uaa
          ServiceInstanceName: produccion-xsuaa-service
          ServiceKeyName: produccion-uaa-key
          sap.cloud.service: produccion
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: produccion-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: produccion-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - produccion.zip
      name: produccion
      target-path: resources/
- name: produccion
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: produccion-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: produccion-destination-service
    service-plan: lite
- name: produccion-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: produccion-xsuaa-service
    service-plan: application
- name: produccion-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: produccion-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
