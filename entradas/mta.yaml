_schema-version: "3.2"
ID: entradas
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: entradas-destination-content
  type: com.sap.application.content
  requires:
  - name: entradas-destination-service
    parameters:
      content-target: true
  - name: entradas-repo-host
    parameters:
      service-key:
        name: entradas-repo-host-key
  - name: entradas-uaa
    parameters:
      service-key:
        name: entradas-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: entradas_html_repo_host
          ServiceInstanceName: entradas-html5-service
          ServiceKeyName: entradas-repo-host-key
          sap.cloud.service: entradas
        - Authentication: OAuth2UserTokenExchange
          Name: entradas_uaa
          ServiceInstanceName: entradas-xsuaa-service
          ServiceKeyName: entradas-uaa-key
          sap.cloud.service: entradas
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: entradas-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: entradas-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - entradas.zip
      name: entradas
      target-path: resources/
- name: entradas
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: entradas-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: entradas-destination-service
    service-plan: lite
- name: entradas-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: entradas-xsuaa-service
    service-plan: application
- name: entradas-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: entradas-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
